import 'package:flutter/material.dart';

class HomeSectionComponent extends StatelessWidget {
  const HomeSectionComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
